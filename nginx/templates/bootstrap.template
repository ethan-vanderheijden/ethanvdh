server {
    listen 80 ;
    listen [::]:80 ;
    server_name containers.${DOMAIN};

    add_header Content-Type text/plain;
    return 200 "";
}

server {
    listen 80 ;
    listen [::]:80 ;
    server_name ${DOMAIN};

    add_header Content-Type text/plain;
    return 200 "";
}
